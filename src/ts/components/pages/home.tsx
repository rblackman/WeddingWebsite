import React, { FC } from 'react';
import Page from '../shared/page';

const Home: FC = () => (
	<Page
		pageName="home"
		heroType="full"
		heroText="#r2b2wedding"
		textX={36}
		textY={65}
		viewBoxX={400}
		viewBoxY={100}
	/>
);

export default Home;
