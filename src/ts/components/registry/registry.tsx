import * as React from 'react';
import { FC } from 'react';

const Registry: FC = () => <p style={{ background: 'rgba(255,0,0, 0.2)', margin: 0, minHeight: '100rem' }}>Registry</p>;

export default Registry;
